import styled from "styled-components";

const BasicBtn = styled.button`
  padding: 1rem;
  margin: 0.5rem;
  outline: none;
  border: none;
`;

export const PrimaryBtn = styled(BasicBtn)`
  background: var(--primary);
`;

export const SecondaryBtn = styled.button``;
